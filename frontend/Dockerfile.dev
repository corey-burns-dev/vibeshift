### development Dockerfile for frontend (Bun + Vite dev server)
### Using official Bun image for better dependency handling and reproducibility
ARG BUN_VERSION=1.3.9
FROM oven/bun:${BUN_VERSION}
WORKDIR /app

# Copy lockfile and package.json first (dependency layer caching)
COPY bun.lock package.json ./

# Install dependencies using Bun
RUN bun install

# Source code will be mounted via volume at runtime
# No COPY needed here - this enables true hot reloading

EXPOSE 5173

# Run Vite dev server using the package script (ensures Bun runs installed deps)
CMD ["bun", "run", "dev"]
